# Airtable
AIRTABLE_API_KEY=your_airtable_api_key
AIRTABLE_BASE_ID=your_airtable_base_id
AIRTABLE_TABLE_LEADS=Leads
AIRTABLE_TABLE_EVENTS=Events

# Admin Metrics (used for /admin/kpi/export authentication)
ADMIN_METRICS_TOKEN=replace_with_long_random_string

# Monitoring & observability
MONITORING_ENV=development
LOG_LEVEL=debug
REQUIRE_AIRTABLE_FOR_READINESS=false

# Regions
NEXT_PUBLIC_DEFAULT_REGION=us
SITE_REGION=us

# SEO
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your_google_site_verification_token

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Member authentication / learning security
# Supabase Auth is the production identity authority. Never store member
# passwords, access tokens, refresh tokens, or raw PINs in localStorage.
# Generate this as a cryptographically random 32+ byte server-only secret.
EDUNANCIAL_CURRICULUM_SECRET=replace_with_at_least_32_random_characters
# Optional server-side pepper used only in addition to a slow password/PIN KDF.
# Do not expose it through NEXT_PUBLIC_* variables.
EDUNANCIAL_MEMBER_PIN_PEPPER=replace_with_at_least_32_random_characters

# CMS Integrations
CMS_BASE_URL=https://your-cms.example.com
STRAPI_API_URL=https://your-strapi.example.com
STRAPI_API_TOKEN=your_strapi_api_token
HYGRAPH_ENDPOINT=https://your-hygraph-endpoint
HYGRAPH_TOKEN=your_hygraph_token

# Square Payment Platform
# Production Application ID from Square Developer Dashboard
NEXT_PUBLIC_SQUARE_APPLICATION_ID=your_square_application_id
# Production Location ID from Square Developer Dashboard
NEXT_PUBLIC_SQUARE_LOCATION_ID=your_square_location_id
# Must be "production" for live payments. Never change to "sandbox" in production.
NEXT_PUBLIC_SQUARE_ENVIRONMENT=production
NEXT_PUBLIC_TRIAL_MEMBERSHIP_ENABLED=false
# Production Access Token from Square Developer Dashboard (server-only, never expose to browser)
SQUARE_ACCESS_TOKEN=your_square_access_token
# Webhook signature key — retrieved automatically from Square API, or set manually if needed
SQUARE_WEBHOOK_SIGNATURE_KEY=your_square_webhook_signature_key
# Webhook notification URL — must match what is registered in your Square application
SQUARE_WEBHOOK_NOTIFICATION_URL=https://edunancial.com/api/square/webhook
# NOTE: SQUARE_VERIFIED_CHECKOUT_ENABLED has been removed. Checkout readiness is
# determined entirely by runtime credential and webhook subscription validation.

# Secure Admin Content Upload Portal
EDUNANCIAL_ADMIN_EMAIL=owner@example.com
EDUNANCIAL_ADMIN_PASSWORD_HASH=
EDUNANCIAL_ADMIN_SESSION_SECRET=replace_with_a_long_random_secret
EDUNANCIAL_UPLOAD_STORAGE_BUCKET=admin-content
EDUNANCIAL_UPLOAD_STORAGE_KEY=admin-content
EDUNANCIAL_GITHUB_TOKEN=
EDUNANCIAL_GITHUB_OWNER=1psiteon1962-stack
EDUNANCIAL_GITHUB_REPO=edunancial-site
EDUNANCIAL_GITHUB_BASE_BRANCH=main
EDUNANCIAL_CU_PASSWORD_HASH=

# AI Learning Network
OPENAI_API_KEY=sk-...
AI_LEARNING_MODEL=gpt-4o-mini
AI_LEARNING_MAX_TOKENS=1200

# Temporary isolated Content Loader portal
EDUNANCIAL_CONTENT_LOADER_PASSWORD=replace_with_temporary_admin_password
EDUNANCIAL_CONTENT_LOADER_USERNAME=content-loader-admin
EDUNANCIAL_CONTENT_LOADER_SESSION_SECRET=replace_with_a_long_random_secret_at_least_32_chars
