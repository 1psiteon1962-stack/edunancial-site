# netlify.toml
[build]
  # No build command – prevents Hugo/Node/etc.
  command = ""
  # Root folder where your site’s static files live
  publish = "public"

# Optional: disable functions since you don’t use them
[functions]
  directory = "disabled"
