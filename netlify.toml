# Netlify config — minimal + safe

[build]
  publish = "."

# Map /api/config -> /config.json (works for both www and root)
[[redirects]]
  from = "/api/config"
  to   = "/config.json"
  status = 200
  force = true
