[build]
  command = ""          # no build step
  publish = "."         # or "public" if your files are in /public

[build.environment]
  NODE_VERSION = "18"   # harmless; prevents odd defaults

[[plugins]]             # ensure no plugins run
  package = "@netlify/plugin-lighthouse"
  inputs = { enabled = false }
