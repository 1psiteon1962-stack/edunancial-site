[build]
  command = ""
  publish = "."

[functions]
  node_bundler = "esbuild"

# Make sure functions resolve under /.netlify/functions/*
[[redirects]]
  from = "/.netlify/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
