{
  "name": "edunancial-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npx velite build && next build",
    "start": "next start",
    "lint": "next lint",
    "velite": "npx velite build"
  },
  "devDependencies": {
    "velite": "^0.1.0"
  }
}
