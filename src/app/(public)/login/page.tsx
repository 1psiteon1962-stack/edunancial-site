import LoginForm from "@/components/auth/LoginForm";

export const metadata = {
  title: "Member Login | Edunancial",
  description: "Sign in to your Edunancial membership account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
