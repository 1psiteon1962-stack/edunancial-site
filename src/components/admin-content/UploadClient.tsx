"use client";

import { useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { ADMIN_CONTENT_LANGUAGES, ADMIN_CONTENT_LANGUAGE_LABELS, type AdminContentLanguage } from "@/lib/admin-content/languages";
import { MEMBERSHIP_ACCESS } from "@/lib/admin-content/constants";
import { runParallelUploads } from "@/lib/admin-content/parallel-upload";

const COURSE_LEVELS = ["level-1", "level-2", "level-3", "level-4", "level-5"] as const;
const MARKETPLACE_CATEGORIES = ["books","ebooks","pdf-guides","templates","worksheets","forms","downloads","zip-packages","audio","videos","images","software","digital-products","calculators","presentations","spreadsheets","future-products"] as const;
type Track = "red" | "white" | "blue" | "green" | "gold" | "purple" | "orange" | "black";
type MembershipAccess = (typeof MEMBERSHIP_ACCESS)[number];
const TRACKS: Array<{value:Track;label:string}> = [{value:"red",label:"🔴 Red — Real Estate"},{value:"white",label:"⚪ White — Paper Assets"},{value:"blue",label:"🔵 Blue — Business"},{value:"green",label:"🟢 Green — Taxes"},{value:"gold",label:"🟡 Gold — Investing"},{value:"purple",label:"🟣 Purple — Law"},{value:"orange",label:"🟠 Orange — Sales & Marketing"},{value:"black",label:"⚫ Black — Leadership & Executive Management"}];
const TRACK_VALUES = new Set(TRACKS.map(track=>track.value));
const FILENAME_LOCALES = ["es-Caribbean","en-US","en-GB","es-ES","fr-CA","fr-FR","pt-BR","pt-PT","de","it","nl","es","fr","pt","en"] as const;
function stemOf(filename:string){return filename.replace(/\.[^.]+$/u,"");}
function filenameTokens(filename:string){return stemOf(filename).toLowerCase().split(/[^a-z0-9]+/u).filter(Boolean);}
function inferLanguage(filename:string){const normalized=`-${stemOf(filename).toLowerCase().replaceAll("_","-").replaceAll(".","-")}-`;return FILENAME_LOCALES.find(locale=>normalized.includes(`-${locale.toLowerCase()}-`))??null;}
function inferTrack(filename:string):Track|null{for(const token of filenameTokens(filename)){if(TRACK_VALUES.has(token as Track))return token as Track;}return null;}
function inferLevel(filename:string):(typeof COURSE_LEVELS)[number]|null{const tokens=filenameTokens(filename);for(let i=0;i<tokens.length;i++){const token=tokens[i];const compact=token.match(/^l([1-5])$/u)??token.match(/^level([1-5])$/u);if(compact)return `level-${compact[1]}` as (typeof COURSE_LEVELS)[number];if(token==="level"&&/^[1-5]$/u.test(tokens[i+1]??""))return `level-${tokens[i+1]}` as (typeof COURSE_LEVELS)[number];}return null;}
function inferTitle(filename:string){const localeTokens=new Set(FILENAME_LOCALES.flatMap(locale=>locale.toLowerCase().split("-")));const metadata=new Set(["complete","combined","package","curriculum",...TRACKS.map(track=>track.value)]);const tokens=stemOf(filename).split(/[-_.\s]+/u).filter(Boolean).filter(token=>{const lower=token.toLowerCase();return !metadata.has(lower)&&!localeTokens.has(lower)&&!/^l[1-5]$/iu.test(token)&&!/^level[1-5]$/iu.test(token)&&!/^[1-5]$/u.test(token);});return tokens.length?tokens.join(" "):null;}
function languageLabel(locale:string){return ADMIN_CONTENT_LANGUAGE_LABELS[locale as AdminContentLanguage]??locale;}
function inspectCurriculumFile(file:File,defaultLanguage:AdminContentLanguage){const track=inferTrack(file.name),level=inferLevel(file.name),explicitLanguage=inferLanguage(file.name),language=explicitLanguage??defaultLanguage,title=inferTitle(file.name);const missing=[!track?"color":null,!level?"level":null].filter((value):value is string=>Boolean(value));return{track,level,language,explicitLanguage,title,missing,ready:missing.length===0};}
function derivedPackageTitle(track:Track,level:(typeof COURSE_LEVELS)[number]){const label=TRACKS.find(entry=>entry.value===track)?.label.replace(/^[^A-Za-z]+/u,"")??track.toUpperCase();return `${label} — ${level.replace("level-","Level ")}`;}

export default function UploadClient(){
 const router=useRouter(),fileInputRef=useRef<HTMLInputElement|null>(null),xhrRefs=useRef<Set<XMLHttpRequest>>(new Set());
 const [files,setFiles]=useState<File[]>([]),[contentDestination,setContentDestination]=useState<""|"courses"|"marketplace">(""),[batchName,setBatchName]=useState(""),[source,setSource]=useState("Admin dashboard"),[notes,setNotes]=useState("");
 const [marketplaceCategory,setMarketplaceCategory]=useState<(typeof MARKETPLACE_CATEGORIES)[number]>("books");
 const [language,setLanguage]=useState<AdminContentLanguage>("en-US"),[membershipAccess,setMembershipAccess]=useState<MembershipAccess>("basic"),[publicationStatus,setPublicationStatus]=useState<"draft"|"review"|"published"|"archived">("draft");
 const [title,setTitle]=useState(""),[description,setDescription]=useState(""),[thumbnailUrl,setThumbnailUrl]=useState(""),[previewUrl,setPreviewUrl]=useState(""),[associatedTrack,setAssociatedTrack]=useState<""|Track>(""),[associatedLevel,setAssociatedLevel]=useState<""|(typeof COURSE_LEVELS)[number]>("");
 const [csrfToken,setCsrfToken]=useState(""),[error,setError]=useState(""),[success,setSuccess]=useState(""),[uploading,setUploading]=useState(false),[progress,setProgress]=useState(0);
 useEffect(()=>{void(async()=>{const response=await fetch("/api/admin/auth/session",{cache:"no-store"});const payload=await response.json();setCsrfToken(payload.csrfToken??"");})();},[]);
 function appendFiles(list:FileList|null){if(!list)return;setFiles(current=>{const seen=new Set(current.map(f=>`${f.name}:${f.size}:${f.lastModified}`)),next=[...current];for(const file of Array.from(list)){const key=`${file.name}:${file.size}:${file.lastModified}`;if(!seen.has(key)){seen.add(key);next.push(file);}}return next;});setError("");}
 function validateBeforeUpload(){if(!contentDestination)return"Choose CONTENT DESTINATION first.";if(contentDestination==="marketplace"&&!title.trim())return"Title is required for Marketplace uploads.";if(contentDestination==="marketplace"&&!description.trim())return"Description is required for Marketplace uploads.";if(files.length===0)return"Select at least one file or ZIP package.";if(contentDestination==="courses"){const unresolved=files.map(file=>({file,identity:inspectCurriculumFile(file,language)})).filter(entry=>!entry.identity.ready);if(unresolved.length)return`Cannot safely classify ${unresolved.length} curriculum package${unresolved.length===1?"":"s"}: ${unresolved.map(entry=>`${entry.file.name} (missing ${entry.identity.missing.join(", ")})`).join("; ")}. Rename the package(s) to include COLOR and LEVEL before uploading.`;}return"";}
 function cancelUploads(){for(const xhr of xhrRefs.current)xhr.abort();xhrRefs.current.clear();}
 function uploadFileDirect(file:File,url:string,method:string,headers:Record<string,string>,onProgress:(loadedBytes:number)=>void):Promise<void>{return new Promise((resolve,reject)=>{const xhr=new XMLHttpRequest();xhrRefs.current.add(xhr);const cleanup=()=>xhrRefs.current.delete(xhr);xhr.upload.onprogress=e=>{if(e.lengthComputable)onProgress(e.loaded);};xhr.onload=()=>{cleanup();xhr.status>=200&&xhr.status<300?resolve():reject(new Error(`Direct upload failed (HTTP ${xhr.status}): ${xhr.responseText}`));};xhr.onerror=()=>{cleanup();reject(new Error("Network error during file upload."));};xhr.onabort=()=>{cleanup();reject(new Error("Upload canceled."));};xhr.open(method,url);Object.entries(headers).forEach(([k,v])=>xhr.setRequestHeader(k,v));xhr.send(file);});}
 async function handleSubmit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const validation=validateBeforeUpload();if(validation){setError(validation);return;}setUploading(true);setError("");setSuccess("");setProgress(0);const firstIdentity=contentDestination==="courses"?inspectCurriculumFile(files[0],language):null;const effectiveTitle=contentDestination==="courses"&&firstIdentity?.track&&firstIdentity.level?derivedPackageTitle(firstIdentity.track,firstIdentity.level):title.trim();const effectiveDescription=contentDestination==="courses"?(description.trim()||"Bulk curriculum package upload; color and level are inferred independently per file, and the selected default language is used only when a filename has no locale."):description.trim();const sharedConfig:Record<string,string>={batchName:batchName||`Content Upload ${new Date().toISOString().slice(0,10)}`,source,notes,contentDestination,membershipAccess,publicationStatus,title:effectiveTitle,description:effectiveDescription,thumbnailUrl,previewUrl};if(contentDestination==="courses"&&firstIdentity?.track&&firstIdentity.level){sharedConfig.courseTrack=firstIdentity.track;sharedConfig.courseLevel=firstIdentity.level;sharedConfig.language=language;}else if(contentDestination==="marketplace"){sharedConfig.language=language;sharedConfig.marketplaceCategory=marketplaceCategory;sharedConfig.associatedTrack=associatedTrack;sharedConfig.associatedLevel=associatedLevel;}let presignResult:{batchId:string;uploads:Array<{uploadId:string;storagePath:string;safeName:string;signedUrl:string|null;directUpload:{url:string;headers:Record<string,string>}|null}>}|null=null;try{const response=await fetch("/api/admin/content/upload/presign",{method:"POST",headers:{"Content-Type":"application/json","x-csrf-token":csrfToken},body:JSON.stringify({...sharedConfig,files:files.map(f=>({name:f.name,size:f.size,type:f.type}))})});if(!response.ok){const parsed=await response.json().catch(()=>({}));throw new Error(parsed.error??"Failed to prepare upload.");}presignResult=await response.json();}catch(err){if(err instanceof Error&&err.message!=="Failed to fetch"){setUploading(false);setError(err.message);return;}}const allDirect=presignResult&&presignResult.uploads.every(u=>u.signedUrl||u.directUpload);if(presignResult&&allDirect){try{await runParallelUploads(files,async(file,i,reportLoadedBytes)=>{const spec=presignResult!.uploads[i];if(spec.signedUrl)await uploadFileDirect(file,spec.signedUrl,"PUT",{"Content-Type":file.type||"application/octet-stream"},reportLoadedBytes);else if(spec.directUpload)await uploadFileDirect(file,spec.directUpload.url,"POST",{...spec.directUpload.headers,"Content-Type":file.type||"application/octet-stream"},reportLoadedBytes);},{sizeOf:file=>file.size,onProgress:({percent})=>setProgress(Math.round(percent*0.85))});setProgress(90);const response=await fetch("/api/admin/content/upload/finalize",{method:"POST",headers:{"Content-Type":"application/json","x-csrf-token":csrfToken},body:JSON.stringify({...sharedConfig,batchId:presignResult.batchId,uploads:files.map((file,i)=>({uploadId:presignResult!.uploads[i].uploadId,originalFilename:file.name,mimeType:file.type||"application/octet-stream",sizeBytes:file.size,storagePath:presignResult!.uploads[i].storagePath}))})});if(!response.ok){const parsed=await response.json().catch(()=>({}));const detail=parsed.detail??parsed.message??parsed.error;throw new Error(detail?`Upload processing failed: ${detail}`:`Upload processing failed during finalization (HTTP ${response.status}).`);}const payload=await response.json();setProgress(100);setSuccess("Upload complete. Opening batch review.");router.push(`/admin/content/batches/${payload.batch.id}`);router.refresh();return;}catch(err){cancelUploads();setUploading(false);setError((err as Error).message);return;}}const max=6*1024*1024,oversized=files.filter(f=>f.size>max);if(oversized.length&&typeof window!=="undefined"&&!window.location.hostname.includes("localhost")){setUploading(false);setError(`Direct Supabase upload is not configured. These files exceed the 6 MB serverless fallback limit: ${oversized.map(f=>f.name).join(", ")}.`);return;}const formData=new FormData();Object.entries(sharedConfig).forEach(([k,v])=>formData.append(k,v));files.forEach(f=>formData.append("files",f));const xhr=new XMLHttpRequest();xhrRefs.current.add(xhr);const cleanup=()=>xhrRefs.current.delete(xhr);xhr.upload.onprogress=e=>{if(e.lengthComputable)setProgress(Math.round(e.loaded/e.total*100));};xhr.onload=()=>{cleanup();setUploading(false);if(xhr.status<200||xhr.status>=300){try{const parsed=JSON.parse(xhr.responseText);setError(parsed.detail??parsed.message??parsed.error??`Upload failed (HTTP ${xhr.status}).`);}catch{setError(`Upload failed (HTTP ${xhr.status}).`);}return;}const payload=JSON.parse(xhr.responseText);router.push(`/admin/content/batches/${payload.batch.id}`);router.refresh();};xhr.onerror=()=>{cleanup();setUploading(false);setError("Network error while uploading.");};xhr.onabort=()=>{cleanup();setUploading(false);setError("Upload canceled.");};xhr.open("POST","/api/admin/content/upload");xhr.setRequestHeader("x-csrf-token",csrfToken);xhr.send(formData);}
 return <form onSubmit={handleSubmit} className="mx-auto max-w-5xl space-y-7"><label className="grid gap-2 text-sm font-bold text-slate-200">CONTENT DESTINATION *<select value={contentDestination} onChange={e=>{setContentDestination(e.target.value as typeof contentDestination);setFiles([]);setError("");}} className="rounded-xl border border-white/10 bg-[#101a2f] px-4 py-3 text-white"><option value="">Select destination</option><option value="courses">COURSES / CURRICULUM</option><option value="marketplace">MARKETPLACE</option></select></label>{!contentDestination?null:<><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Batch name<input value={batchName} onChange={e=>setBatchName(e.target.value)} className="rounded-xl bg-[#101a2f] px-4 py-3" placeholder="Mixed curriculum upload"/></label><label className="grid gap-2 text-sm font-semibold">Source<input value={source} onChange={e=>setSource(e.target.value)} className="rounded-xl bg-[#101a2f] px-4 py-3"/></label></div>{contentDestination==="courses"?<div className="rounded-2xl border border-blue-500/20 bg-blue-500/5 p-5"><p className="font-black text-blue-200">Automatic per-package curriculum classification</p><p className="mt-2 text-sm text-slate-300">Color and Level are determined independently for every selected package. Choose the default language below for canonical filenames that do not contain a locale; an explicit locale in a filename overrides it.</p><label className="mt-4 grid gap-2 text-sm font-semibold">Default language for untagged curriculum files *<select value={language} onChange={e=>setLanguage(e.target.value as AdminContentLanguage)} className="rounded-xl bg-[#101a2f] px-4 py-3 text-white">{ADMIN_CONTENT_LANGUAGES.map(locale=><option key={locale} value={locale}>{ADMIN_CONTENT_LANGUAGE_LABELS[locale]}</option>)}</select></label></div>:<div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Batch / Marketplace Title *<input value={title} onChange={e=>setTitle(e.target.value)} className="rounded-xl bg-[#101a2f] px-4 py-3"/></label><label className="grid gap-2 text-sm font-semibold">Language *<select value={language} onChange={e=>setLanguage(e.target.value as AdminContentLanguage)} className="rounded-xl bg-[#101a2f] px-4 py-3">{ADMIN_CONTENT_LANGUAGES.map(locale=><option key={locale} value={locale}>{ADMIN_CONTENT_LANGUAGE_LABELS[locale]}</option>)}</select></label></div>}<div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Membership Access *<select value={membershipAccess} onChange={e=>setMembershipAccess(e.target.value as MembershipAccess)} className="rounded-xl bg-[#101a2f] px-4 py-3">{MEMBERSHIP_ACCESS.map(tier=><option key={tier} value={tier}>{tier.charAt(0).toUpperCase()+tier.slice(1)}</option>)}</select></label><label className="grid gap-2 text-sm font-semibold">Publication Status *<select value={publicationStatus} onChange={e=>setPublicationStatus(e.target.value as typeof publicationStatus)} className="rounded-xl bg-[#101a2f] px-4 py-3"><option value="draft">Draft</option><option value="review">Review</option><option value="published">Published</option><option value="archived">Archived</option></select></label></div>{contentDestination==="marketplace"?<><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Description *<textarea value={description} onChange={e=>setDescription(e.target.value)} className="min-h-28 rounded-xl bg-[#101a2f] px-4 py-3"/></label><label className="grid gap-2 text-sm font-semibold">Category *<select value={marketplaceCategory} onChange={e=>setMarketplaceCategory(e.target.value as typeof marketplaceCategory)} className="rounded-xl bg-[#101a2f] px-4 py-3">{MARKETPLACE_CATEGORIES.map(category=><option key={category} value={category}>{category}</option>)}</select></label></div><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Associated color<select value={associatedTrack} onChange={e=>setAssociatedTrack(e.target.value as typeof associatedTrack)} className="rounded-xl bg-[#101a2f] px-4 py-3"><option value="">None</option>{TRACKS.map(track=><option key={track.value} value={track.value}>{track.label}</option>)}</select></label><label className="grid gap-2 text-sm font-semibold">Associated level<select value={associatedLevel} onChange={e=>setAssociatedLevel(e.target.value as typeof associatedLevel)} className="rounded-xl bg-[#101a2f] px-4 py-3"><option value="">None</option>{COURSE_LEVELS.map(level=><option key={level} value={level}>{level.replace("level-","Level ")}</option>)}</select></label></div></>:null}<div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Thumbnail URL<input value={thumbnailUrl} onChange={e=>setThumbnailUrl(e.target.value)} className="rounded-xl bg-[#101a2f] px-4 py-3"/></label><label className="grid gap-2 text-sm font-semibold">Preview URL<input value={previewUrl} onChange={e=>setPreviewUrl(e.target.value)} className="rounded-xl bg-[#101a2f] px-4 py-3"/></label></div><label className="grid gap-2 text-sm font-semibold">Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} className="min-h-24 rounded-xl bg-[#101a2f] px-4 py-3"/></label><div className="rounded-2xl border border-dashed border-white/20 bg-white/5 p-6"><input ref={fileInputRef} type="file" multiple className="hidden" onChange={e=>appendFiles(e.target.files)}/><button type="button" onClick={()=>fileInputRef.current?.click()} className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Choose files / ZIP packages</button><p className="mt-3 text-sm text-slate-400">Curriculum filenames must identify color and level. Locale is optional when the selected default language is correct.</p>{files.length?<div className="mt-4 space-y-2">{files.map((file,index)=>{const identity=contentDestination==="courses"?inspectCurriculumFile(file,language):null;return <div key={`${file.name}:${file.lastModified}`} className="flex flex-wrap items-center justify-between gap-3 rounded-lg bg-black/20 px-3 py-2 text-sm"><span>{file.name}</span>{identity?<span className={identity.ready?"text-emerald-300":"text-amber-300"}>{identity.track??"?"} / {identity.level?.replace("level-","L")??"?"} / {languageLabel(identity.language)}{identity.explicitLanguage?" (filename)":" (default)"}</span>:null}<button type="button" onClick={()=>setFiles(current=>current.filter((_,i)=>i!==index))} className="text-red-300">Remove</button></div>;})}</div>:null}</div>{uploading?<div><div className="h-2 overflow-hidden rounded bg-white/10"><div className="h-full bg-blue-500" style={{width:`${progress}%`}}/></div><p className="mt-2 text-sm">Uploading… {progress}%</p><button type="button" onClick={cancelUploads} className="mt-2 text-sm text-red-300">Cancel upload</button></div>:null}{error?<p className="rounded-xl border border-red-500/30 bg-red-500/10 p-4 text-sm text-red-200">{error}</p>:null}{success?<p className="rounded-xl border border-emerald-500/30 bg-emerald-500/10 p-4 text-sm text-emerald-200">{success}</p>:null}<button type="submit" disabled={uploading||!contentDestination} className="rounded-xl bg-emerald-600 px-6 py-3 font-black text-white disabled:opacity-50">{uploading?"Uploading…":"Upload content"}</button></>}</form>;
}
