"use client";

import { useEffect, useRef, useState } from "react";

type TransitionType = "cut" | "fade" | "wipeleft" | "wiperight" | "slideleft" | "slideright";
type SceneDraft = { file: File; assetId?: string; durationSeconds: number; overlayText: string; fitMode: "contain" | "cover"; transitionType: TransitionType; transitionSeconds: number };
const TTS_LOCALES = [
  ["en-US","English (United States)"],["en-GB","English (United Kingdom)"],["es-US","Spanish (United States)"],["es-PR","Spanish (Puerto Rico)"],["es-DO","Spanish (Dominican Republic)"],["es-MX","Spanish (Mexico)"],["es-ES","Spanish (Spain)"],["fr-CA","French (Canada)"],["fr-FR","French (France)"],["pt-BR","Portuguese (Brazil)"],["pt-PT","Portuguese (Portugal)"],["de-DE","German"],["it-IT","Italian"],["nl-NL","Dutch"],["ja-JP","Japanese"],["ko-KR","Korean"],["sw-UG","Swahili (East Africa)"],["ar-SA","Arabic"],
] as const;
const TTS_VOICES = ["coral","alloy","ash","ballad","echo","fable","nova","onyx","sage","shimmer"] as const;
function csrfToken(){const m=document.cookie.match(/(?:^|; )edunancial_admin_csrf=([^;]+)/u);return m?decodeURIComponent(m[1]):"";}
async function jsonRequest(url:string,init:RequestInit){const r=await fetch(url,{...init,headers:{"content-type":"application/json","x-csrf-token":csrfToken(),...(init.headers??{})},cache:"no-store"});const d=await r.json().catch(()=>({}));if(!r.ok||d.success===false)throw new Error(d.error||`Request failed (${r.status}).`);return d;}
async function uploadSigned(url:string,file:Blob,mime:string){const r=await fetch(url,{method:"PUT",headers:{"content-type":mime},body:file});if(!r.ok)throw new Error(`Private asset upload failed (${r.status}).`);}

export default function MarketingShortClient(){
 const[title,setTitle]=useState("");const[scenes,setScenes]=useState<SceneDraft[]>([]);const[narration,setNarration]=useState<Blob|null>(null);const[narrationName,setNarrationName]=useState("narration.webm");const[narrationLocale,setNarrationLocale]=useState("en-US");const[narrationVolume,setNarrationVolume]=useState(1);const[ttsScript,setTtsScript]=useState("");const[ttsVoice,setTtsVoice]=useState("coral");const[ttsBusy,setTtsBusy]=useState(false);const[music,setMusic]=useState<File|null>(null);const[musicVolume,setMusicVolume]=useState(.18);const[recording,setRecording]=useState(false);const[projectId,setProjectId]=useState<string|null>(null);const[jobId,setJobId]=useState<string|null>(null);const[status,setStatus]=useState("Ready");const[outputUrl,setOutputUrl]=useState<string|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState<string|null>(null);const recorderRef=useRef<MediaRecorder|null>(null),chunksRef=useRef<Blob[]>([]);
 useEffect(()=>{if(!jobId)return;let stopped=false;const poll=async()=>{try{const r=await fetch(`/api/admin/video/jobs/${jobId}`,{cache:"no-store"}),d=await r.json();if(stopped)return;if(d?.status)setStatus(d.status);if(d?.lastError)setError(d.lastError);if(d?.outputUrl)setOutputUrl(d.outputUrl);if(d?.status==="succeeded"||d?.status==="failed")stopped=true;}catch{}};void poll();const t=window.setInterval(()=>{if(!stopped)void poll();},2500);return()=>{stopped=true;window.clearInterval(t);};},[jobId]);
 function addFiles(files:FileList|null){if(!files)return;const next=Array.from(files).slice(0,Math.max(0,30-scenes.length)).map(file=>({file,durationSeconds:6,overlayText:"",fitMode:"contain" as const,transitionType:"cut" as const,transitionSeconds:.35}));setScenes(c=>[...c,...next]);}
 function moveScene(i:number,d:-1|1){const t=i+d;if(t<0||t>=scenes.length)return;const c=[...scenes];[c[i],c[t]]=[c[t],c[i]];setScenes(c);}
 async function startRecording(){setError(null);try{const stream=await navigator.mediaDevices.getUserMedia({audio:true}),rec=new MediaRecorder(stream);chunksRef.current=[];rec.ondataavailable=e=>{if(e.data.size)chunksRef.current.push(e.data);};rec.onstop=()=>{setNarration(new Blob(chunksRef.current,{type:rec.mimeType||"audio/webm"}));setNarrationName(`narration-${Date.now()}.webm`);stream.getTracks().forEach(t=>t.stop());};recorderRef.current=rec;rec.start();setRecording(true);}catch(c){setError(c instanceof Error?c.message:"Microphone access failed.");}}
 function stopRecording(){recorderRef.current?.stop();setRecording(false);}
 async function generateTts(){if(!ttsScript.trim())return;setTtsBusy(true);setError(null);try{const response=await fetch("/api/admin/video/tts",{method:"POST",headers:{"content-type":"application/json","x-csrf-token":csrfToken()},body:JSON.stringify({script:ttsScript.trim(),locale:narrationLocale,voice:ttsVoice})});if(!response.ok){const d=await response.json().catch(()=>({}));throw new Error(d.error||`Text-to-speech failed (${response.status}).`);}const blob=await response.blob();setNarration(blob);setNarrationName(`edunancial-narration-${narrationLocale}-${Date.now()}.mp3`);setStatus(`Narration generated: ${narrationLocale} / ${ttsVoice}`);}catch(c){setError(c instanceof Error?c.message:"Text-to-speech generation failed.");}finally{setTtsBusy(false);}}
 function resetStudio(){setTitle("");setScenes([]);setNarration(null);setNarrationName("narration.webm");setNarrationLocale("en-US");setNarrationVolume(1);setTtsScript("");setTtsVoice("coral");setMusic(null);setMusicVolume(.18);setProjectId(null);setJobId(null);setOutputUrl(null);setError(null);setStatus("Ready");}
 async function createShort(){if(!title.trim()||!scenes.length)return;setBusy(true);setError(null);setOutputUrl(null);try{setStatus("Creating project…");const first=scenes[0].file,created=await jsonRequest("/api/admin/video/projects",{method:"POST",body:JSON.stringify({title:title.trim(),fileName:first.name,mimeType:first.type||"application/octet-stream",byteSize:first.size})});setProjectId(created.projectId);await uploadSigned(created.signedUploadUrl,first,first.type||"application/octet-stream");const uploaded=[...scenes];uploaded[0]={...uploaded[0],assetId:created.assetId};for(let i=1;i<uploaded.length;i++){setStatus(`Uploading scene ${i+1} of ${uploaded.length}…`);const f=uploaded[i].file,a=await jsonRequest(`/api/admin/video/projects/${created.projectId}/assets`,{method:"POST",body:JSON.stringify({fileName:f.name,mimeType:f.type||"application/octet-stream",byteSize:f.size})});await uploadSigned(a.signedUploadUrl,f,f.type||"application/octet-stream");uploaded[i]={...uploaded[i],assetId:a.assetId};}setScenes(uploaded);let narrationAssetId:string|null=null;if(narration){setStatus("Uploading narration…");const mime=narration.type||"audio/webm",a=await jsonRequest(`/api/admin/video/projects/${created.projectId}/assets`,{method:"POST",body:JSON.stringify({fileName:narrationName,mimeType:mime,byteSize:narration.size})});await uploadSigned(a.signedUploadUrl,narration,mime);narrationAssetId=a.assetId;}let musicAssetId:string|null=null;if(music){setStatus("Uploading background music…");const mime=music.type||"audio/mpeg",a=await jsonRequest(`/api/admin/video/projects/${created.projectId}/assets`,{method:"POST",body:JSON.stringify({fileName:music.name,mimeType:mime,byteSize:music.size})});await uploadSigned(a.signedUploadUrl,music,mime);musicAssetId=a.assetId;}setStatus("Saving composition…");await jsonRequest(`/api/admin/video/projects/${created.projectId}/composition`,{method:"PUT",body:JSON.stringify({scenes:uploaded.map(s=>({assetId:s.assetId,durationSeconds:s.durationSeconds,overlayText:s.overlayText,fitMode:s.fitMode,transitionType:s.transitionType,transitionSeconds:s.transitionSeconds})),narration:narrationAssetId?{assetId:narrationAssetId,locale:narrationLocale,volume:narrationVolume}:null,backgroundMusic:musicAssetId?{assetId:musicAssetId,locale:"und",volume:musicVolume}:null})});setStatus("Starting 9:16 render…");const q=await jsonRequest("/api/admin/video/jobs",{method:"POST",body:JSON.stringify({projectId:created.projectId,editRecipe:{trimStart:0,trimEnd:null,durationSeconds:uploaded[0].durationSeconds}})});setJobId(q.jobId);setStatus("queued");}catch(c){setError(c instanceof Error?c.message:"Could not create Marketing Short.");setStatus("Failed");}finally{setBusy(false);}}
 return <section className="grid gap-6 lg:grid-cols-[1.25fr_0.75fr]"><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-6 sm:p-8"><h2 className="text-2xl font-black">Marketing Short editor</h2><p className="mt-2 text-sm leading-6 text-slate-400">Build a vertical Short from up to 30 scenes, narration, transitions, text overlays, and optional background music.</p><label className="mt-6 block text-sm font-bold text-slate-200">Project title</label><input value={title} onChange={e=>setTitle(e.target.value)} maxLength={200} className="mt-2 w-full rounded-xl border border-white/15 bg-slate-950/50 px-4 py-3"/><label className="mt-5 block text-sm font-bold text-slate-200">Artwork / video scenes</label><input type="file" multiple accept="image/png,image/jpeg,image/webp,video/mp4,video/webm" onChange={e=>addFiles(e.target.files)} className="mt-2 block w-full rounded-xl border border-dashed border-white/20 bg-slate-950/30 p-4 text-sm"/><div className="mt-4 space-y-3">{scenes.map((s,i)=><div key={`${s.file.name}-${i}`} className="rounded-2xl border border-white/10 bg-slate-950/40 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><p className="font-bold">Scene {i+1}: {s.file.name}</p><div className="flex gap-2"><button type="button" onClick={()=>moveScene(i,-1)} className="rounded-lg border border-white/15 px-3 py-2 text-xs">Up</button><button type="button" onClick={()=>moveScene(i,1)} className="rounded-lg border border-white/15 px-3 py-2 text-xs">Down</button><button type="button" onClick={()=>setScenes(c=>c.filter((_,x)=>x!==i))} className="rounded-lg border border-red-400/30 px-3 py-2 text-xs text-red-200">Remove</button></div></div><div className="mt-3 grid gap-3 sm:grid-cols-3"><label className="text-xs text-slate-400">Duration (sec)<input type="number" min={1} max={60} value={s.durationSeconds} onChange={e=>setScenes(c=>c.map((x,n)=>n===i?{...x,durationSeconds:Number(e.target.value)}:x))} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2"/></label><label className="text-xs text-slate-400">Fit<select value={s.fitMode} onChange={e=>setScenes(c=>c.map((x,n)=>n===i?{...x,fitMode:e.target.value as "contain"|"cover"}:x))} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2"><option value="contain">Contain</option><option value="cover">Cover</option></select></label><label className="text-xs text-slate-400">Optional overlay<input value={s.overlayText} onChange={e=>setScenes(c=>c.map((x,n)=>n===i?{...x,overlayText:e.target.value}:x))} maxLength={500} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2"/></label></div>{i<scenes.length-1?<div className="mt-3 grid gap-3 sm:grid-cols-2"><label className="text-xs text-slate-400">Transition to next scene<select value={s.transitionType} onChange={e=>setScenes(c=>c.map((x,n)=>n===i?{...x,transitionType:e.target.value as TransitionType}:x))} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2"><option value="cut">Cut</option><option value="fade">Crossfade</option><option value="wipeleft">Wipe left</option><option value="wiperight">Wipe right</option><option value="slideleft">Slide left</option><option value="slideright">Slide right</option></select></label>{s.transitionType!=="cut"?<label className="text-xs text-slate-400">Transition duration: {s.transitionSeconds.toFixed(2)} sec<input type="range" min="0.1" max="1.5" step="0.05" value={s.transitionSeconds} onChange={e=>setScenes(c=>c.map((x,n)=>n===i?{...x,transitionSeconds:Number(e.target.value)}:x))} className="mt-2 w-full"/></label>:null}</div>:null}</div>)}</div><div className="mt-6 rounded-2xl border border-blue-400/20 bg-blue-950/20 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-bold">Narration</p><p className="text-xs text-slate-400">Generate multilingual speech, record your voice, or upload audio.</p></div>{!recording?<button type="button" onClick={startRecording} className="rounded-lg bg-blue-400 px-4 py-2 text-sm font-black text-slate-950">Start recording</button>:<button type="button" onClick={stopRecording} className="rounded-lg bg-red-400 px-4 py-2 text-sm font-black text-slate-950">Stop recording</button>}</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs text-slate-300">Language / locale<select value={narrationLocale} onChange={e=>setNarrationLocale(e.target.value)} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2">{TTS_LOCALES.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label className="text-xs text-slate-300">Voice<select value={ttsVoice} onChange={e=>setTtsVoice(e.target.value)} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2">{TTS_VOICES.map(v=><option key={v} value={v}>{v}</option>)}</select></label></div><label className="mt-3 block text-xs text-slate-300">TTS script<textarea value={ttsScript} onChange={e=>setTtsScript(e.target.value)} maxLength={4000} rows={5} placeholder="Paste the narration script in the language you want spoken." className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2"/></label><div className="mt-2 flex items-center justify-between gap-3 text-xs text-slate-400"><span>{ttsScript.length}/4000 characters</span><button type="button" disabled={ttsBusy||!ttsScript.trim()} onClick={generateTts} className="rounded-lg bg-cyan-300 px-4 py-2 font-black text-slate-950 disabled:opacity-40">{ttsBusy?"Generating…":"Generate narration"}</button></div><p className="mt-2 text-xs text-slate-500">The script is spoken as written; selecting a locale guides pronunciation and delivery but does not translate the text.</p><input type="file" accept="audio/*" onChange={e=>{const f=e.target.files?.[0];if(f){setNarration(f);setNarrationName(f.name);}}} className="mt-4 block w-full text-sm"/>{narration?<><audio controls src={URL.createObjectURL(narration)} className="mt-3 w-full"/><p className="mt-2 text-xs text-green-300">Narration ready: {narrationName} · {narrationLocale}</p><label className="mt-3 block text-xs text-slate-300">Narration volume: {Math.round(narrationVolume*100)}%<input type="range" min="0" max="2" step="0.05" value={narrationVolume} onChange={e=>setNarrationVolume(Number(e.target.value))} className="mt-1 w-full"/></label></>:null}</div><div className="mt-4 rounded-2xl border border-purple-400/20 bg-purple-950/20 p-4"><p className="font-bold">Background music</p><p className="mt-1 text-xs text-slate-400">Upload music you have permission to use. It will be mixed underneath narration.</p><input type="file" accept="audio/*" onChange={e=>setMusic(e.target.files?.[0]??null)} className="mt-3 block w-full text-sm"/>{music?<><p className="mt-2 text-xs text-green-300">Music ready: {music.name}</p><label className="mt-3 block text-xs text-slate-300">Music volume: {Math.round(musicVolume*100)}%<input type="range" min="0" max="1" step="0.01" value={musicVolume} onChange={e=>setMusicVolume(Number(e.target.value))} className="mt-1 w-full"/></label></>:null}</div><button type="button" disabled={busy||!scenes.length||!title.trim()} onClick={createShort} className="mt-7 w-full rounded-xl bg-yellow-400 px-5 py-4 text-base font-black text-slate-950 disabled:opacity-40">{busy?"Preparing…":`Render ${scenes.length||""} Scene Marketing Short`}</button></div><aside className="rounded-3xl border border-white/10 bg-slate-950/40 p-6 sm:p-8"><p className="text-xs font-black uppercase tracking-[0.25em] text-blue-300">Render status</p><p className="mt-4 text-2xl font-black capitalize">{status}</p>{outputUrl?<video src={outputUrl} controls playsInline preload="metadata" className="mt-6 aspect-[9/16] max-h-[520px] w-full rounded-2xl border border-white/10 bg-black object-contain"/>:<div className="mt-6 aspect-[9/16] max-h-[520px] rounded-2xl border border-white/10 bg-black/40 p-5 text-center text-sm text-slate-400"><div className="flex h-full items-center justify-center">1080 × 1920 vertical output</div></div>}{outputUrl?<div className="mt-5 grid gap-3"><a href={outputUrl} download={`${title.trim().replace(/[^a-z0-9]+/giu,"-").replace(/^-|-$/gu,"")||"edunancial-short"}.mp4`} className="rounded-xl bg-green-400 px-5 py-3 text-center font-black text-slate-950">Download MP4</a><button type="button" onClick={resetStudio} className="rounded-xl border border-white/15 px-5 py-3 font-bold">Create another video</button></div>:null}{projectId?<p className="mt-4 break-all text-xs text-slate-500">Project: {projectId}</p>:null}{jobId?<p className="mt-1 break-all text-xs text-slate-500">Job: {jobId}</p>:null}{error?<div className="mt-5 rounded-xl border border-red-400/30 bg-red-950/30 p-4 text-sm text-red-200">{error}</div>:null}</aside></section>;
}
