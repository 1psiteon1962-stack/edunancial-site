/**
 * Region-based curriculum content.
 *
 * This module has been migrated to the canonical i18n pipeline.
 * All curriculum strings are now served from src/locales/<code>.json
 * using the canonical translate() / useInternationalPreferences().t() function.
 *
 * Use the following locale keys:
 *   curriculum.global.title
 *   curriculum.global.description
 */
export {};
