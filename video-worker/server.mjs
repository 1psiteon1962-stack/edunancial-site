import http from "node:http";
import { createHash, createHmac, timingSafeEqual } from "node:crypto";
import { mkdir, mkdtemp, readFile, rm, stat, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { dirname, join, normalize } from "node:path";
import { spawn } from "node:child_process";
import { createClient } from "@supabase/supabase-js";

const PORT=Number(process.env.PORT||8080),SUPABASE_URL=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL||"",SUPABASE_SERVICE_ROLE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||"",WORKER_SHARED_SECRET=(process.env.WORKER_SHARED_SECRET||"").trim(),MEDIA_ROOT=process.env.VIDEO_MEDIA_ROOT||"/tmp/edunancial-video-media";const activeJobs=new Set();
function requireConfig(){if(!SUPABASE_URL)throw new Error("SUPABASE_URL is required for Video Maker metadata");if(!SUPABASE_SERVICE_ROLE_KEY)throw new Error("SUPABASE_SERVICE_ROLE_KEY is required for Video Maker metadata");if(WORKER_SHARED_SECRET.length<32)throw new Error("WORKER_SHARED_SECRET must be at least 32 characters");}
function json(res,status,payload){const body=JSON.stringify(payload);res.writeHead(status,{"content-type":"application/json","content-length":Buffer.byteLength(body),"access-control-allow-origin":"*","access-control-allow-methods":"GET,PUT,OPTIONS","access-control-allow-headers":"content-type"});res.end(body);}
function safeMediaPath(value){const clean=normalize(String(value||"").replace(/^\/+/,""));if(!clean||clean.startsWith("..")||clean.includes("/../"))throw new Error("Invalid media path");return join(MEDIA_ROOT,clean);}
function mediaSignature(method,path,expires){return createHmac("sha256",WORKER_SHARED_SECRET).update(`${method.toUpperCase()}\n${path}\n${expires}`).digest("hex");}
function verifyMedia(method,path,url){const expires=Number(url.searchParams.get("expires")),sig=url.searchParams.get("sig")||"";if(!Number.isFinite(expires)||expires<Math.floor(Date.now()/1000))return false;const expected=mediaSignature(method,path,expires),a=Buffer.from(sig),b=Buffer.from(expected);return a.length===b.length&&timingSafeEqual(a,b);}
function collectBuffer(req,max=250*1024*1024){return new Promise((resolve,reject)=>{const chunks=[];let size=0;req.on("data",c=>{size+=c.length;if(size>max){reject(new Error("Media upload too large"));req.destroy();return;}chunks.push(c);});req.on("end",()=>resolve(Buffer.concat(chunks)));req.on("error",reject);});}
function collect(req){return collectBuffer(req,1024*1024).then(b=>b.toString("utf8"));}
function verifySignature(req,path,body){const timestamp=req.headers["x-edunancial-timestamp"],requestId=req.headers["x-edunancial-request-id"],signature=req.headers["x-edunancial-signature"];if(typeof timestamp!=="string"||typeof requestId!=="string"||typeof signature!=="string")throw new Error("Missing worker authentication headers");const age=Math.abs(Math.floor(Date.now()/1000)-Number(timestamp));if(!Number.isFinite(age)||age>300)throw new Error("Worker request timestamp is stale");const bodyHash=createHash("sha256").update(body).digest("hex"),canonical=[timestamp,requestId,req.method.toUpperCase(),path,bodyHash].join("\n"),expected=createHmac("sha256",WORKER_SHARED_SECRET).update(canonical).digest("hex"),a=Buffer.from(signature),b=Buffer.from(expected);if(a.length!==b.length||!timingSafeEqual(a,b))throw new Error("Invalid worker signature");return requestId;}
function run(cmd,args,label=cmd){return new Promise((resolve,reject)=>{const child=spawn(cmd,args,{stdio:["ignore","ignore","pipe"]});let stderr="";child.stderr.on("data",d=>stderr=(stderr+d.toString()).slice(-16000));child.on("error",reject);child.on("close",(code,signal)=>code===0?resolve():reject(new Error(`${label} failed (${signal||code}): ${stderr.slice(-5000)}`)));});}
async function fetchAsset(supabase,asset,filePath){if(asset.storage_bucket==="worker-media"){await writeFile(filePath,await readFile(safeMediaPath(asset.storage_path)));return;}const {data,error}=await supabase.storage.from(asset.storage_bucket).download(asset.storage_path);if(error||!data)throw new Error(error?.message||"Could not download legacy media");await writeFile(filePath,Buffer.from(await data.arrayBuffer()));}
async function executeJob(jobId,requestId){requireConfig();const supabase=createClient(SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const {error:replayError}=await supabase.from("video_worker_requests").insert({request_id:requestId,job_id:jobId});if(replayError)throw new Error(`Duplicate or invalid worker request: ${replayError.message}`);const {data:job,error:jobError}=await supabase.from("video_jobs").select("id,project_id,status,attempt_count").eq("id",jobId).single();if(jobError||!job)throw new Error(jobError?.message||"Video job not found");if(job.status==="succeeded")return;await supabase.from("video_jobs").update({status:"processing",started_at:new Date().toISOString(),attempt_count:Number(job.attempt_count||0)+1,last_error:null}).eq("id",jobId);await supabase.from("video_projects").update({status:"processing",updated_at:new Date().toISOString()}).eq("id",job.project_id);const dir=await mkdtemp(join(tmpdir(),"edunancial-video-"));try{const {data:scenes,error:scenesError}=await supabase.from("video_scenes").select("scene_order,duration_seconds,fit_mode,video_assets(storage_bucket,storage_path,mime_type)").eq("project_id",job.project_id).order("scene_order");if(scenesError||!scenes?.length)throw new Error(scenesError?.message||"No video scenes found");const sceneFiles=[];for(const [index,scene] of scenes.entries()){const asset=Array.isArray(scene.video_assets)?scene.video_assets[0]:scene.video_assets;if(!asset?.storage_path)throw new Error(`Scene ${index+1} asset unavailable`);const input=join(dir,`input-${index}`),normalized=join(dir,`normalized-${index}.png`),output=join(dir,`scene-${index}.mp4`);await fetchAsset(supabase,asset,input);const duration=Math.max(1,Math.min(60,Number(scene.duration_seconds||6))),isImage=String(asset.mime_type||"").startsWith("image/");let renderInput=input;if(isImage){await run("ffmpeg",["-hide_banner","-loglevel","error","-y","-i",input,"-vf","scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2:black","-frames:v","1","-threads","1",normalized],`scene ${index+1} normalize`);await rm(input,{force:true});renderInput=normalized;}const inputArgs=isImage?["-loop","1","-t",String(duration),"-i",renderInput]:["-i",renderInput,"-t",String(duration)];const vf=isImage?"fps=30,format=yuv420p":"scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2:black,fps=30,format=yuv420p";await run("ffmpeg",["-hide_banner","-loglevel","error","-y",...inputArgs,"-vf",vf,"-an","-c:v","libx264","-preset","ultrafast","-threads","1","-filter_threads","1","-pix_fmt","yuv420p","-movflags","+faststart",output],`scene ${index+1} render`);await rm(renderInput,{force:true});sceneFiles.push(output);}const listFile=join(dir,"concat.txt");await writeFile(listFile,sceneFiles.map(s=>`file '${s.replace(/'/g,"'\\''")}'`).join("\n"));const visual=join(dir,"visual.mp4");await run("ffmpeg",["-hide_banner","-loglevel","error","-y","-f","concat","-safe","0","-i",listFile,"-c","copy","-movflags","+faststart",visual],"scene assembly");for(const f of sceneFiles)await rm(f,{force:true});const {data:tracks,error:audioError}=await supabase.from("video_audio_tracks").select("track_type,volume,muted,video_assets(storage_bucket,storage_path,mime_type)").eq("project_id",job.project_id).order("created_at");if(audioError)throw new Error(audioError.message);const narration=(tracks||[]).find(t=>!t.muted&&["ORIGINAL_NARRATION","TRANSLATED_NARRATION"].includes(t.track_type)),music=(tracks||[]).find(t=>!t.muted&&t.track_type==="BACKGROUND_MUSIC"),master=join(dir,"master.mp4");if(!narration&&!music)await run("ffmpeg",["-y","-i",visual,"-c","copy","-movflags","+faststart",master],"final copy");else{const args=["-hide_banner","-loglevel","error","-y","-i",visual],filters=[];let idx=1,nLabel=null,mLabel=null;for(const track of[narration,music]){if(!track)continue;const a=Array.isArray(track.video_assets)?track.video_assets[0]:track.video_assets;if(!a?.storage_path)continue;const local=join(dir,`audio-${idx}`);await fetchAsset(supabase,a,local);args.push("-i",local);const label=track.track_type==="BACKGROUND_MUSIC"?"music":"narr";filters.push(`[${idx}:a]volume=${Number(track.volume??1)}[${label}]`);if(label==="music")mLabel=`[${label}]`;else nLabel=`[${label}]`;idx++;}let map=nLabel||mLabel;if(nLabel&&mLabel){filters.push(`${nLabel}${mLabel}amix=inputs=2:duration=longest:dropout_transition=2[aout]`);map="[aout]";}if(filters.length)args.push("-filter_complex",filters.join(";"));args.push("-map","0:v:0");if(map)args.push("-map",map);args.push("-c:v","copy","-c:a","aac","-b:a","128k","-threads","1","-shortest","-movflags","+faststart",master);await run("ffmpeg",args,"audio mix");}await run("ffprobe",["-v","error","-select_streams","v:0","-show_entries","stream=codec_name,width,height","-of","default=noprint_wrappers=1",master],"output validation");const info=await stat(master),storagePath=`projects/${job.project_id}/processed/${job.id}-master.mp4`,target=safeMediaPath(storagePath);await mkdir(dirname(target),{recursive:true});await writeFile(target,await readFile(master));const {data:out,error:assetError}=await supabase.from("video_assets").insert({project_id:job.project_id,asset_type:"EDITED_MASTER",storage_bucket:"worker-media",storage_path:storagePath,original_filename:`${job.id}-master.mp4`,mime_type:"video/mp4",byte_size:info.size}).select("id").single();if(assetError||!out)throw new Error(assetError?.message||"Could not register rendered master");await supabase.from("video_jobs").update({status:"succeeded",output_asset_id:out.id,completed_at:new Date().toISOString(),last_error:null}).eq("id",job.id);await supabase.from("video_projects").update({status:"master_ready",updated_at:new Date().toISOString()}).eq("id",job.project_id);}catch(error){const message=error instanceof Error?error.message:"Video render failed";await supabase.from("video_jobs").update({status:"failed",last_error:message.slice(0,4000),completed_at:new Date().toISOString()}).eq("id",jobId);await supabase.from("video_projects").update({status:"failed",updated_at:new Date().toISOString()}).eq("id",job.project_id);throw error;}finally{await rm(dir,{recursive:true,force:true});}}
function launch(jobId,requestId){if(activeJobs.has(jobId))return false;activeJobs.add(jobId);setImmediate(()=>executeJob(jobId,requestId).catch(e=>console.error(`video job ${jobId} failed:`,e.message)).finally(()=>activeJobs.delete(jobId)));return true;}
const server=http.createServer(async(req,res)=>{try{if(req.method==="OPTIONS")return json(res,204,{});const url=new URL(req.url||"/",`http://${req.headers.host||"localhost"}`);if(req.method==="GET"&&url.pathname==="/health"){requireConfig();return json(res,200,{ok:true,service:"edunancial-video-worker",activeJobs:activeJobs.size,mediaBackend:"worker-media"});}if(url.pathname.startsWith("/media/")){const path=decodeURIComponent(url.pathname.slice(7));if(!verifyMedia(req.method||"GET",path,url))return json(res,403,{ok:false,error:"Invalid or expired media signature"});const file=safeMediaPath(path);if(req.method==="PUT"){const bytes=await collectBuffer(req);await mkdir(dirname(file),{recursive:true});await writeFile(file,bytes);return json(res,201,{ok:true,size:bytes.length});}if(req.method==="GET"){const bytes=await readFile(file);res.writeHead(200,{"content-type":path.endsWith(".mp4")?"video/mp4":"application/octet-stream","content-length":bytes.length,"content-disposition":path.endsWith(".mp4")?`attachment; filename="${path.split("/").pop()}"`:"inline","access-control-allow-origin":"*"});return res.end(bytes);}}const match=url.pathname.match(/^\/internal\/jobs\/([0-9a-f-]{36})\/execute$/i);if(req.method==="POST"&&match){const body=await collect(req),requestId=verifySignature(req,url.pathname,body),parsed=JSON.parse(body||"{}");if(parsed.jobId!==match[1])return json(res,400,{ok:false,error:"Job ID mismatch"});const accepted=launch(match[1],requestId);return json(res,202,{ok:true,status:accepted?"accepted":"already_processing",jobId:match[1]});}return json(res,404,{ok:false,error:"Not found"});}catch(error){return json(res,500,{ok:false,error:error instanceof Error?error.message:"Worker failure"});}});server.requestTimeout=15*60*1000;server.headersTimeout=20000;server.keepAliveTimeout=5000;server.listen(PORT,"0.0.0.0",()=>console.log(`edunancial-video-worker listening on ${PORT}`));